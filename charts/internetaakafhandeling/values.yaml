# Naming overrides - useful when used as subchart
nameOverride: ""
fullnameOverride: ""

# Pod scheduling controls applied to both cronjobs and web-deployment.
# Defaults are empty so behavior is unchanged on single-nodepool clusters.
# Useful for multi-nodepool clusters (e.g. AKS system/user pool separation)
# where workloads must be pinned to specific nodes.
nodeSelector: {}
tolerations: []
affinity: {}

# Global PostgreSQL configuration
postgresql:
  enabled: true
  image:
    repository: bitnamilegacy/postgresql
  auth:
    username: ""
    password: ""
    database: "interneTaakAfhandeling"
    postgresPassword: ""
  primary:
    persistence:
      enabled: true
      size: 8Gi
  metrics:
    enabled: false
    image:
      repository: bitnamilegacy/postgres-exporter
  volumePermissions:
    image:
      repository: bitnamilegacy/os-shell

# Ingress Configuration
ingress:
  enabled: false
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  className: ""
  # -- ingress hosts
  hosts: []
  #  - host: ita.gemeente.nl
  #    paths:
  #      - path: /
  #        pathType: ImplementationSpecific
  tls: []
  #  - secretName: ita-tls
  #    hosts:
  #      - ita.gemeente.nl

# Database Configuration
database:
  # When postgresql.enabled=true, host is auto-set to {Release.Name}-postgresql
  # Specify this host only when postgresql.enabled=false
  host: ""
  port: "5432"
  name: "interneTaakAfhandeling"
  username: ""
  password: ""

# API Connections
apiConnections:
  openKlant:
    baseUrl: ""
    apiKey: ""
  object:
    baseUrl: ""
    apiKey: ""
  zaakSysteem:
    baseUrl: ""
    clientId: ""
    key: ""

logboek:
  # -- De url van het logboek objecttype in de objecttypen api. zie de objecttypen pagina in de documentatie for meer informatie
  type: ""
  # -- De versie van het logboek objecttype dat gebruikt wordt (hoogstwaarschijnlijk 1)
  typeVersion: 1

afdeling:
  # -- De url van het afdeling objecttype in de objecttypen api. zie de objecttypen pagina in de documentatie for meer informatie
  type: ""
  # -- De versie van het afdeling objecttype dat gebruikt wordt (hoogstwaarschijnlijk 1)
  typeVersion: 1

groep:
  # -- De url van het groep objecttype in de objecttypen api. zie de objecttypen pagina in de documentatie for meer informatie
  type: ""
  # -- De versie van het groep objecttype dat gebruikt wordt (hoogstwaarschijnlijk 1)
  typeVersion: 1

medewerker:
  # -- De url van het medewerker objecttype in de objecttypen api. zie de objecttypen pagina in de documentatie voor meer informatie
  type: ""
  # -- De versie van het medewerker objecttype dat gebruikt wordt (hoogstwaarschijnlijk 1)
  typeVersion: 1

smtp:
  host: ""
  port: "25"
  username: ""
  password: ""
  fromEmail: ""
  enableSsl: "true"

# ITA Base URL for email deeplinks
# Example: https://itawebsite.nl
ita:
  baseUrl: ""

# Web Application Configuration
web:
  image:
    repository: "ghcr.io/interne-taak-afhandeling/internetaakafhandeling.web"
    tag: "latest"
    pullPolicy: Always
  
  service:
    type: ClusterIP
    port: 80
  
  oidc:
    authority: ""
    clientId: ""
    clientSecret: ""
    itaSystemAccessRole: ""
    functioneelBeheerderRole: ""
    nameClaimType: ""
    roleClaimType: ""
    objectregisterMedewerkerIdClaimType: ""
    emailClaimType: ""
  
  styling:
    logoUrl: ""
    faviconUrl: ""
    designTokensUrl: ""
    webFontSources: []

  resources: {}
  # limits:
  #   cpu: 200m
  #   memory: 256Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

  appsettings:
    setting1: ""
    setting2: ""

# Poller — shared image and resource settings for all poller CronJobs
poller:
  image:
    repository: "ghcr.io/interne-taak-afhandeling/internetaakafhandeling.poller"
    tag: "latest"
    pullPolicy: Always

  resources: {}
  # limits:
  #   cpu: 200m
  #   memory: 256Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

# NieuweInternetaakNotificatie — notifies assignees when a new internetaak is created (every 15 min)
nieuweInternetaakNotificatie:
  schedule: "*/15 * * * *"
  notification:
    pollerMessage: "Poller uitgevoerd om:"
    hourThreshold: "-24"

# VerlopenContactverzoekHerinnering — sends a daily reminder for overdue contact requests (weekdays 07:00)
verlopenContactverzoekHerinnering:
  enabled: true
  schedule: "0 7 * * 1-5"
